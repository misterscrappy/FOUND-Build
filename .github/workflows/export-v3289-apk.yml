name: Export FOUND v3.2.89 APK

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/export-v3289-apk.yml'

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout bridge
        uses: actions/checkout@v4

      - name: Checkout private FOUND
        uses: actions/checkout@v4
        with:
          repository: misterscrappy/FOUND
          ref: main
          token: ${{ secrets.FOUND_PRIVATE_REPO_TOKEN }}
          path: source
          persist-credentials: false

      - name: Verify v3.2.89 APK
        run: |
          set -euo pipefail
          APK='source/builds/FOUND-v3.2.89-full-game.apk'
          test -s "$APK"
          test "$(sha256sum "$APK" | awk '{print $1}')" = '585ba8f27930f660cd4ede1e23fced96cd28f906569a90a660ed8c6390a983ca'
          cp "$APK" FOUND-v3.2.89-full-game.apk
          sha256sum FOUND-v3.2.89-full-game.apk > FOUND-v3.2.89-full-game.apk.sha256

      - name: Upload verified APK
        uses: actions/upload-artifact@v4
        with:
          name: FOUND-v3.2.89-full-game-APK
          path: |
            FOUND-v3.2.89-full-game.apk
            FOUND-v3.2.89-full-game.apk.sha256
          if-no-files-found: error
          retention-days: 1
